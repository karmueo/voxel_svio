# Voxel-SVIO V1_01_easy 误差分析

## 评估配置

- ROS 环境：
  - `source /opt/ros/jazzy/setup.bash`
  - `source install/setup.bash`
- 评估命令：

```bash
ros2 run voxel_svio error_singlerun \
  posyaw \
  /home/scl/work/slam/open_vins/ov_data/euroc_mav/V1_01_easy.txt \
  /tmp/voxel_svio_v1_01/pose.txt
```

- 真值文件：`/home/scl/work/slam/open_vins/ov_data/euroc_mav/V1_01_easy.txt`
- 估计文件：`/tmp/voxel_svio_v1_01/pose.txt`
- 轨迹格式：`timestamp tx ty tz qx qy qz qw`
- 对齐方式：`posyaw`，即估计轨迹按位置和 yaw 与真值轨迹对齐，尺度保持为 `1`，再计算 ATE、RPE 和 NEES。

## 数据覆盖情况

| 项目 | 数值 |
| --- | ---: |
| 真值轨迹样本数 | 2895 |
| 估计轨迹样本数 | 2524 |
| 真值时间范围 | 1403715273.262140 - 1403715417.962140 s |
| 估计时间范围 | 1403715278.612143 - 1403715418.812143 s |
| 估计轨迹相对真值起始延迟 | 5.350003 s |
| 时间戳关联样本数 | 2509 |
| 实际评估覆盖时长 | 139.350000 s |
| 最大时间戳关联偏差 | 0.000003099 s |
| 平均时间戳关联偏差 | 0.000003020 s |
| 真值轨迹长度 | 58.353 m |
| 估计轨迹长度 | 58.456 m |
| 匹配段真值轨迹长度 | 58.301 m |
| 匹配段估计轨迹长度 | 58.453 m |
| `posyaw` 对齐 yaw | 13.541221 deg |

估计轨迹没有覆盖真值最开始约 `5.35 s` 的片段，因此当前统计主要反映 VIO 初始化完成后的重叠轨迹段。时间戳关联最大偏差约 `3.1 us`，远小于 `error_singlerun` 使用的 `0.02 s` 阈值，时间同步本身不是当前误差的主要来源。

估计轨迹长度与匹配段真值长度接近，说明尺度误差较小。当前对齐 yaw 约为 `13.54 deg`，该值主要用于消除估计坐标系与真值坐标系之间的全局 yaw 差异。

## 绝对轨迹误差

| 指标 | 姿态误差 deg | 位置误差 m |
| --- | ---: | ---: |
| RMSE | 0.736 | 0.063 |
| Mean | 0.639 | 0.057 |
| Min | 0.009 | 0.013 |
| Max | 2.391 | 0.145 |
| Std | 0.366 | 0.027 |

位置 RMSE 为 `6.3 cm`，平均位置误差为 `5.7 cm`，最大位置误差约 `14.5 cm`。相对 `58.301 m` 的匹配段真值轨迹长度，绝对位置 RMSE 约为 `0.11%`，位置估计整体较稳定。

姿态 RMSE 为 `0.736 deg`，平均姿态误差为 `0.639 deg`，最大值为 `2.391 deg`。这里的 `rmse_ori` 单位是角度，不是弧度；`0.736 deg` 约等于 `0.0129 rad`。该姿态误差不能和 `rmse_pos = 0.063 m` 直接按数值大小比较。

## 启动段影响

| 跳过起始时长 | 样本数 | 姿态 RMSE deg | 位置 RMSE m | 最大姿态误差 deg |
| ---: | ---: | ---: | ---: | ---: |
| 0 s | 2509 | 0.736 | 0.063 | 2.391 |
| 5 s | 2419 | 0.685 | 0.059 | 1.852 |
| 10 s | 2329 | 0.624 | 0.057 | 1.721 |
| 15 s | 2239 | 0.587 | 0.055 | 1.105 |
| 20 s | 2149 | 0.582 | 0.055 | 1.105 |
| 30 s | 1969 | 0.564 | 0.056 | 1.105 |

姿态误差的大值主要集中在评估起始阶段。跳过前 `15 s` 后，姿态 RMSE 从 `0.736 deg` 降至 `0.587 deg`，最大姿态误差从 `2.391 deg` 降至 `1.105 deg`。这说明当前姿态误差更像是初始化后早期收敛和少量局部峰值造成的结果，而不是全程稳定存在的坐标系或时间同步错误。

## 相对位姿误差

| 片段长度 m | 姿态中位误差 deg | 位置中位误差 m | 样本数 |
| ---: | ---: | ---: | ---: |
| 8 | 0.706 | 0.050 | 2145 |
| 16 | 0.674 | 0.061 | 1871 |
| 24 | 0.746 | 0.075 | 1631 |
| 32 | 0.781 | 0.094 | 1274 |
| 40 | 0.873 | 0.110 | 983 |

RPE 位置中位误差随片段长度增加而逐步上升，说明局部运动误差存在一定距离累积。40 m 片段的位置中位误差为 `10.97 cm`，相对片段长度约 `0.27%`。姿态中位误差从 8 m 的 `0.706 deg` 增加到 40 m 的 `0.873 deg`，长片段上姿态漂移仍处于较低量级。

## NEES 情况

评估输出提示估计轨迹中没有协方差信息：

```text
[TRAJ]: Normalized Estimation Error Squared called but trajectory does not have any covariances...
[TRAJ]: Did you record using a Odometry or PoseWithCovarianceStamped????
```

因此当前 NEES 输出全为 `0`，不能用于判断滤波器一致性。若需要分析一致性，需要导出带姿态和位置协方差的估计轨迹。

## 姿态误差说明

本次 `rmse_ori = 0.736` 并不是异常大的弧度误差，而是 OpenVINS 评估定义下的角度误差。OpenVINS 的 ATE 姿态误差定义为：

```text
ori_err = 180 / pi * norm(log_so3(R_est_aligned^T * R_gt))
```

当前 `voxel_svio` 输出的 `pose.txt` 使用 `q_GtoI`，与 OpenVINS 真值文件的 JPL 四元数约定匹配。若错误地把四元数按相反方向或 Hamilton/JPL 符号混用解释，姿态 RMSE 会达到约 `68 deg`，而不是当前的 `0.736 deg`。因此当前姿态误差主要来自估计本身的 roll、pitch、yaw 残差，尤其是启动后早期收敛段的局部峰值。

## 结论

当前 Voxel-SVIO 在 EuRoC `V1_01_easy` 上的 `error_singlerun posyaw` 结果整体较好：绝对位置 RMSE 约 `6.3 cm`，绝对姿态 RMSE 约 `0.74 deg`。时间戳匹配误差只有微秒级，四元数约定与 OpenVINS 评估一致，`rmse_ori` 偏高的直观感受主要来自单位误解和启动段峰值。

需要注意的是，本次估计轨迹比真值晚开始约 `5.35 s`，评估不包含真值最初阶段；同时当前轨迹文件没有协方差，无法进行 NEES 一致性分析。后续若要更完整比较初始化质量和滤波一致性，建议导出覆盖完整序列且包含协方差的轨迹，并分别统计全程和跳过启动段后的误差。
